import 'package:flutter/material.dart';

class UnknownPathView extends StatelessWidget {
  const UnknownPathView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
