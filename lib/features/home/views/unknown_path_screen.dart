import 'package:flutter/material.dart';

class UnknownPathScreen extends StatelessWidget {
  const UnknownPathScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
